{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UbicacionBandeja list</h1>

    <table class="records_list table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cantidad</th>
                <th>Tipo de Bandeja</th>
                <th>Ubicación</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ubicacionbandeja_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.cantidad|number_format(0, '.', '.') }}</td>
                <td>{{ entity.bandeja }}</td>
                <td>{{ entity.tipoUbicacion }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('ubicacionbandeja_show', { 'id': entity.id }) }}">Ver</a>
                        </li>
                        <li>
                            <a href="{{ path('ubicacionbandeja_edit', { 'id': entity.id }) }}">Editar</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ubicacionbandeja_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
