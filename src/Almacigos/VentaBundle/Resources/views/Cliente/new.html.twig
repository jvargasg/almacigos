{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ingresar nuevo Cliente</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('cliente') }}">
            Volver al listado
        </a>
    </li>
</ul>
{% endblock %}
