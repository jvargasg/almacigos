{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Contado list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Iva</th>
                <th>Porcentaje del total</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('contado_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>${{ entity.iva|number_format(0, '.', '.') }}</td>
                <td>{{ entity.porcentajeDelTotal }}%</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('contado_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('contado_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('contado_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
