{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ingreso de Semilla</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('semilla') }}">
            Volver al listado
        </a>
    </li>
</ul>
{% endblock %}
