{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Credito creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('credito') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
