<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Almacigos SA - {% block title %}{% endblock %}</title>
        <!-- Bootstrap core CSS -->
        {#<link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css" rel="stylesheet">#}
        <link href="{{ asset('bundles/estilo/bootstrap.min.css') }}" rel="stylesheet" type="text/css" media="all">
        <link href="{{ asset('bundles/estilo/estilo.css') }}" rel="stylesheet" type="text/css" media="all">  
        {% block stylesheets %}
            
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>

    <body class="container">

        <nav class="navbar navbar-default" role="navigation">
            <div class="container-fluid">

                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="{{ path('usuario_login') }}">
                        <img alt="Allmacigos SA" src="{{ asset('bundles/estilo/img/logo.gif') }}">
                    </a>         
                </div>

                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                    <ul class="nav navbar-nav">
                        <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Administración <span class="caret"></span></a>
                            <ul class="dropdown-menu" role="menu">
                                <li>
                                    <a href="{{ path('fechasiembra') }}">Fechas de inicio de temporadas</a>
                                </li>
                                <li>
                                    <a href="{{ path('iva')}}">IVA</a>
                                </li>
                                <li role="presentation" class="{% if bandeja_active is defined  %} {{bandeja_active}} {% endif %}" >
                                    <a href="{{ path('bandeja') }}">Bandejas</a>
                                </li>
                                <li role="presentation" class="{% if tipoUbicacion_active is defined  %} {{tipoUbicacion_active}} {% endif %}" >
                                    <a href="{{ path('tipoubicacion') }}">Ubicaciones de bandejas</a>
                                </li>
                                <li role="presentation" class="{% if producto_active is defined  %} {{producto_active}} {% endif %}" >
                                    <a href="{{ path('producto') }}">Productos</a>
                                </li>
                                <li role="presentation" class="{% if semilla_active is defined  %} {{semilla_active}} {% endif %}" >
                                    <a href="{{ path('semilla') }}">Semillas</a>
                                </li>
                                <li role="presentation" class="{% if cliente_active is defined  %} {{cliente_active}} {% endif %}" >
                                    <a href="{{ path('cliente') }}">Clientes</a>
                                </li>
                            </ul>
                        </li>
                        <li role="presentation" class="{% if notaventa_active is defined  %} {{notaventa_active}} {% endif %}" >
                            <a href="{{ path('notaventa') }}">Notas de Ventas</a></li>
                        <li role="presentation" class="{% if pedido_active is defined  %} {{pedido_active}} {% endif %}" >
                            <a href="{{ path('pedido') }}">Pedidos</a>
                        </li>
                        <li role="presentation" class="{% if siembra_active is defined  %} {{siembra_active}} {% endif %}" >
                            <a href="{{ path('siembra') }}">Siembras</a>
                        </li>                                                
                        <li><a href="">Reportes</a></li>
                    </ul>
                </div>

            </div>
        </nav>

        {% block body %}
            
        {% endblock %}
        
        <script src="{{ asset('bundles/estilo/jquery.min.js') }}"></script>
        <script src="{{ asset('bundles/estilo/bootstrap.min.js') }}"></script> 
        {% block javascripts %}            
        {% endblock %}

    </body>

</html>
